import { Grid } from '@mui/material';
import React from 'react';

const MeterMaster = () => {
	return (
		<Grid container xs={12} sm={6}>
			This is meter master
		</Grid>
	);
};

export default MeterMaster;
